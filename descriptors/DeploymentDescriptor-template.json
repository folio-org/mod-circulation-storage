{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -jar ../mod-circulation-storage/target/mod-circulation-storage-fat.jar -Dhttp.port=%p embed_postgres=true"
  }
}
