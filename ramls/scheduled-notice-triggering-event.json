{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "string",
  "description": "Scheduled notice triggering event",
  "enum": [
    "Due date",
    "Hold Expiration",
    "Request expiration"
  ]
}
